- Improves performance when swiping through screens
- Adds validations so we can now indicate which are required fields
- Fixes follow-up subscriptions
- Failed follow-up subscriptions are now cached for retry (retry not implemented yet)
- Fixed several memory leaks
